# 2026-07-12 project review and phased remediation plan

## Purpose

This document converts the 2026-07-12 repository review into an ordered,
implementation-ready remediation plan. It fixes the decisions needed to resolve
the concerns so implementation agents do not have to choose product behavior,
compatibility policy, security posture, persistence semantics, or test scope.

The requirements, accepted ADRs, and policies in `AGENTS.md` remain the source
of truth. If an implementation detail below conflicts with a requirement or an
accepted ADR, stop that phase, correct this review and the affected ADR first,
and then resume. Do not silently choose a third behavior.

This is not an MVP plan. Every required capability remains part of the release
contract. Progress is measured only by completed behavior and acceptance
criteria; no calendar estimates belong in this document or derived work.

## Status vocabulary

Only these phase states are used:

- `TODO`: any implementation, test, documentation, ADR, or quality-gate item in
  the phase is incomplete. Work in progress remains `TODO`.
- `COMPLETE`: every task and every phase-specific and global definition-of-done
  item has passed, and the evidence links are in the phase map.

Do not mark a phase `COMPLETE` based on code alone. Do not add partial,
in-progress, blocked, deferred, or not-applicable states to the phase map.

## Phase map

Phases are ordered by dependency. A phase may use completed work from an
earlier phase, but it must not implement around an unfinished prerequisite.

| Phase | Status | Concern IDs | Required outcome | Completion evidence |
| --- | --- | --- | --- | --- |
| 0. Review baseline and remediation specification | COMPLETE | All | Concerns reproduced, decisions fixed, and implementation phases defined | This document and the baseline checks below |
| 1. Bound and harden untrusted metadata parsing | TODO | C-01 | Every metadata ingress and durable-state decode is bounded, checked, and panic-free | Add test names and ADR link when complete |
| 2. Make containment fail safe by default and on live path loss | TODO | C-02 | An omitted path cannot enable torrent traffic, and active traffic terminates when the required path fails | Add unit, injected-transition, Linux integration, and ADR evidence when complete |
| 3. Apply browser-origin protection to every control API | TODO | C-03 | Native and compatibility mutation endpoints enforce one same-origin policy in both authentication modes | Add route-matrix test and ADR-update evidence when complete |
| 4. Complete seeding policy, lifecycle, accounting, and live upload shaping | TODO | C-04, C-09a | Per-torrent seed policy works end to end; production seeding state, file progress, statistics, and live limits are truthful | Add lifecycle/local-swarm/API/UI and ADR evidence when complete |
| 5. Enforce one process-wide peer connection budget | TODO | C-05 | The configured global peer cap covers inbound and outbound peer sessions across all torrents | Add concurrency and reconfiguration evidence when complete |
| 6. Make watch-folder ingestion stable, idempotent, and transactional | TODO | C-06 | Partially written files are not read, unchanged files are not retried forever, and failed persistence leaves no ghost torrent | Add filesystem/failure-injection and ADR evidence when complete |
| 7. Make contained HTTP framing, redirects, ranges, and tracker scrape complete | TODO | C-07, C-09b | Trackers and webseeds use a standards-aware HTTP/1.1 layer over contained sockets, and supported tracker scrape is real | Add local HTTP/HTTPS protocol and ADR/dependency evidence when complete |
| 8. Decompose oversized modules and remove duplicate daemon compilation | TODO | C-08 | Runtime, engine, API, and Web UI modules have explicit ownership and the binary consumes the library facade | Add module-boundary and unchanged-behavior evidence when complete |
| 9. Reconcile requirement evidence and close release readiness | TODO | C-09 | Every completion claim maps to reachable production behavior and an acceptance test; version and release docs reflect compatibility impact | Add tracker links, full quality logs, and release-version evidence when complete |

When a phase becomes complete, replace its placeholder in the final column with
links to its tests, ADRs, and documentation. Change only that row's status from
`TODO` to `COMPLETE`.

## Baseline reviewed on 2026-07-12

The review found a clean worktree and a passing baseline. These results show
that the concerns are behavioral and architectural gaps rather than an
already-failing build:

- `cargo fmt --all -- --check`
- `cargo check --workspace --all-targets`
- `cargo test --workspace --all-targets`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `node --check crates/swarmotter-web/assets/app.js`
- `node --check crates/swarmotter-web/assets/theme-bootstrap.js`
- `mdbook build`
- `GLUETUN_ENV_FILE=gluetun.env.example docker compose --env-file deploy/.env.example -f deploy/compose.yml config`

Ignored scale/soak tests and a privileged live-path-loss test are not covered by
that baseline. They are explicit completion gates below.

## Concern inventory

### C-01: untrusted metainfo can consume unbounded stack, memory, and disk-read resources

`crates/swarmotter-core/src/bencode.rs` accepts recursive input without a depth
or node budget, performs unchecked string-bound arithmetic, and does not require
the decoder to consume the entire input. `crates/swarmotter-core/src/meta.rs`
does not impose a complete metainfo shape budget. Engine paths narrow the
declared piece length and allocate piece buffers after parsing. Watch-folder
imports read an entire file before applying the API/BEP 9 metadata-size policy.
Malformed durable piece-hash data can reach an exact-slice copy without first
checking the decoded length.

### C-02: the default containment posture and live failure behavior do not meet the fail-closed claim

`NetworkConfig::default()` selects disabled containment while Serde's partial
`[network]` default selects strict mode. Starting without a configuration file
therefore enables the torrent data plane without an explicit operator choice,
and getting-started documentation recommends disabled mode. The live health
loop constructs its own OS probe, is not deterministically testable, and uses
normal shutdown paths after a required interface disappears. Existing UDP
sockets do not re-check a shared containment gate on send. Public statuses
`socket_bind_failed` and `blocked_fail_closed` are not reached by production
status transitions.

### C-03: compatibility APIs bypass the native API's browser-origin defense

The `/transmission/rpc` and `/api/v2` qBittorrent-compatible routes are mounted
outside the middleware that validates `Origin`, `Host`, and Fetch Metadata for
`/api/v1`. When authentication is intentionally disabled, form-compatible
mutation endpoints can accept cross-site browser requests. This diverges from
ADR-0049's same-origin protection requirement.

### C-04: per-torrent seeding behavior exists as pure logic but is not a product capability

The core has `TorrentSeeding`, but `Torrent` does not persist it and the daemon
always evaluates `TorrentSeeding::default()`. No native endpoint or Web UI
control edits it. Production completion starts a seeder without transitioning
the torrent to `seeding`, so `active_seeds` can remain zero. A seeder receives a
new upload limiter at start, while a live per-torrent limit update changes only
the downloader's limiter. Required ratio, idle, seed-forever, stop-target, live
shaping, and truthful lifecycle behavior are therefore not complete.

### C-05: `bandwidth.max_peers` is divided into estimates, not enforced globally

`effective_peer_worker_limit` rounds a global value into at least one worker per
active torrent. More active torrents than the configured cap can exceed it.
Inbound seeding sessions use a separate allowance, so inbound and outbound
sessions are not counted against one process-wide budget even though the
configuration documentation describes a total connection cap.

### C-06: watch-folder import is immediate, repeatable, blocking, and non-transactional

The pure stable-size helper is not used by the daemon. A discovered file is read
immediately with blocking filesystem calls in an async scan. `leave` files are
reprocessed every scan, including duplicate failures. Registry and queue state
change and scheduling can begin before persistence succeeds, with no rollback.
Post-import moves ignore errors and can overwrite or strand operator files
depending on platform behavior. Import history is unbounded.

### C-07: the contained HTTP reader is not a complete HTTP/1.1 implementation

Tracker and webseed traffic obtains sockets through containment, but the custom
reader treats EOF as message framing and does not decode chunked bodies, honor
`Content-Length`, implement bounded redirects, or construct every authority
correctly for explicit ports and IPv6 literals. Tracker code treats redirects
as response bodies. Webseed range verification cannot work reliably with
chunked transfer coding.

### C-08: oversized modules and duplicate declarations increase regression risk

`crates/swarmotterd/src/daemon.rs`, `crates/swarmotterd/src/engine.rs`,
`crates/swarmotter-api/src/handlers/torrents.rs`, and
`crates/swarmotter-web/assets/app.js` combine unrelated responsibilities.
`swarmotterd/src/main.rs` redeclares modules already exposed by `lib.rs`, so
daemon source and unit tests compile through both binary and library targets.

### C-09: release-completion evidence is ahead of reachable production behavior

The v1 completion tracker marks behavior complete where the inspected runtime
does not expose it end to end. Besides C-02 through C-07, examples include
tracker scrape without a scrape request implementation, per-file
`bytes_completed` remaining at initialization values, and lifecycle/status
variants that are modeled but not produced. A passing pure-helper test is not
evidence that its product path is complete.

## Execution rules for all implementation phases

1. Implement phases in numerical order. A later phase may prepare a fixture,
   but it must not merge a workaround for an unfinished earlier phase.
2. Preserve lawful-use and containment rules. Network tests use generated
   torrents, local peers, and local HTTP/HTTPS services. No third-party tracker,
   peer, webseed, index, magnet, or copyrighted fixture is permitted.
3. Add or update tests with behavior. Tests must exercise the production call
   path when the concern is about integration; helper tests are supplementary.
4. Create each prescribed ADR before or alongside implementation. Use
   `Proposed` while incomplete and `Accepted` only in the change that satisfies
   the phase definition of done. Filenames assume ADR-0049 remains last. If an
   ADR lands first, renumber new ADRs mechanically to the next sequential
   numbers while retaining titles and order; do not change their decisions.
5. Update `CHANGELOG.md` under the unreleased section by completed capability,
   not internal refactor. Do not add release or duration estimates.
6. Do not add a dependency unless a phase prescribes it. Record each prescribed
   dependency's license, purpose, direct/transitive status, and containment
   impact in `THIRD_PARTY_LICENSES.md`.
7. Preserve backward durable-state decoding unless explicitly stated. Never
   silently discard invalid records; return a typed error with record context.
8. Use the central binder for every torrent socket, DNS lookup, and HTTP
   connection. The control-plane listener remains separate.

## Phase 0: review baseline and remediation specification

**Status: COMPLETE**

The review inspected requirements, accepted ADRs, configuration and operator
documentation, core parsing/model code, daemon lifecycle/network/storage paths,
native and compatibility API routing, Web UI assets, tests, packaging, and the
completion tracker. It reproduced the concerns in C-01 through C-09 by tracing
each claim from product documentation to its production call path. It made no
runtime or product changes.

The baseline commands listed above passed, the worktree was clean, and this
document fixes phase order, behavior, tests, documentation work, ADR work, and
quality gates. Those artifacts are the completion evidence for Phase 0.

### Phase 0 definition of done

Concern evidence is recorded, implementation choices are fixed, every concern
maps to an ordered phase, and later phases have explicit tests, documentation,
and global quality requirements. Phase 0 is complete by this document.

## Phase 1: bound and harden untrusted metadata parsing

**Status: TODO**

**Decision record:** create
`design/adr/0050-bounded-untrusted-metainfo-parsing.md`. Record that limits
apply equally to API uploads, watch folders, magnet metadata, restored state,
and direct core parser callers.

### Required implementation

Define these public constants in `crates/swarmotter-core/src/meta.rs` and use
them instead of duplicate literals:

```rust
pub const MAX_TORRENT_METADATA_BYTES: usize = 16 * 1024 * 1024;
pub const MAX_BENCODE_DEPTH: usize = 128;
pub const MAX_BENCODE_NODES: usize = 250_000;
pub const MAX_TORRENT_FILES: usize = 100_000;
pub const MAX_TORRENT_PIECES: usize = 750_000;
pub const MAX_PIECE_LENGTH: u64 = 64 * 1024 * 1024;
```

Apply this exact parser contract in `bencode.rs`:

- Reject input larger than `MAX_TORRENT_METADATA_BYTES` before parsing.
- Count the root as depth zero. Increment on entering a list/dictionary and
  reject an entry that would exceed `MAX_BENCODE_DEPTH`.
- Count every integer, byte string, list, and dictionary as one node and reject
  the node that would exceed `MAX_BENCODE_NODES`.
- Use `checked_add` for every cursor/length calculation and verify the end is no
  greater than input length before slicing or allocating.
- Reject empty integers, leading zeroes other than `0`, negative zero, missing
  terminators, non-string dictionary keys, and duplicate dictionary keys.
  Continue accepting unsorted unique keys for interoperability; info-hash
  calculation still uses the original encoded `info` slice.
- Require EOF after exactly one top-level value. Trailing bytes are an error.
- Return `CoreError::Bencode` or `CoreError::MalformedTorrent` with a short
  reason. No malformed input may panic.

Apply these metainfo checks before constructing `TorrentMeta`:

- `piece length` is in `1..=MAX_PIECE_LENGTH`.
- The pieces string is an exact multiple of 20 and its hash count does not
  exceed `MAX_TORRENT_PIECES`.
- File count does not exceed `MAX_TORRENT_FILES`.
- Total-length and file-offset additions are checked.
- Empty torrents are invalid. A non-empty torrent provides exactly
  `ceil(total_length / piece_length)` hashes.

Use `u32::try_from(meta.piece_length)` at every engine/storage boundary that
needs `u32`; return `MalformedTorrent` instead of narrowing with `as`. Allocate
no piece-sized buffer until limits and conversions pass.

Before reading a watch file, check metadata length, allocate only the checked
length, and use a bounded read that rejects growth over the limit. Keep the same
limit for API and BEP 9 metadata even when `api.max_request_body_bytes` is
higher for other requests.

In durable-state deserialization, require each decoded SHA-1 piece hash to be
exactly 20 bytes before copying. Include torrent record and piece index in the
error without payload data or content paths.

### Required tests

- Depth 128 accepted and depth 129 rejected.
- Exactly `MAX_BENCODE_NODES` accepted when otherwise valid and the next node
  rejected.
- Overflowing/beyond-input lengths, missing terminators, duplicate keys,
  invalid integer forms, and trailing bytes.
- Metadata at the byte limit and one byte over through core parser, API add,
  watch import, and BEP 9 assembly.
- Piece length zero/over-limit, mismatched piece count, too many files/hashes,
  and total-length overflow.
- Durable piece hashes of 0, 19, 20, and 21 decoded bytes; only 20 succeeds.
- Every malformed corpus case wrapped in `std::panic::catch_unwind`.

Build boundary cases in tests from small generated lawful data; do not commit
large binary fixtures.

### Documentation updates

- Document the 16 MiB limit and rejection response in `design/api.md` and
  `docs/api.md`.
- Document watch-folder use of the limit in `design/configuration.md` and
  `docs/configuration.md`.
- Add parser budgets/adversarial cases to `design/testing.md`.
- Identify the shared parser trust boundary in `design/architecture.md`.
- Record the completed capability in `CHANGELOG.md`.

### Phase 1 definition of done

Production ingress tests pass; malformed data causes typed errors and no panic
or oversized allocation; ADR-0050 and docs match code constants; and every
global quality gate passes.

## Phase 2: make containment fail safe by default and on live path loss

**Status: TODO**

**Decision record:** create
`design/adr/0051-explicit-network-path-and-live-containment-gate.md`. It
supersedes only earlier text permitting an omitted network table to select
disabled containment. Link ADR-0005, ADR-0012, and ADR-0047 explicitly.

### Required configuration behavior

Make `NetworkConfig::default().mode` `strict`, matching the Serde default. An
omitted `[network]` table produces strict mode without a path and fails
`Config::validate()` with `invalid_config` before the control listener or any
background task starts. `--check-config` must fail the same way.

Disabled containment remains available only through this explicit setting:

```toml
[network]
mode = "disabled"
```

This mode is for development or a separately enforced boundary such as the
supplied Gluetun shared-network-namespace deployment. Never infer disabled mode
from a missing file/table, platform, bind failure, or unavailable interface.
Never auto-change strict to preferred or disabled.

Move full config validation before logging initialization and before the
`--check-config` success message. A run without `--config` fails unless env
overrides provide a valid strict path or explicit disabled mode.

### Required live containment architecture

Introduce one process-wide `ContainmentGate`, owned by `DaemonRuntime` and
shared by every binder, DHT runner, listener, engine, seeder, tracker, webseed,
and metadata task. Use atomics plus `tokio::sync::Notify`, not a new dependency.
It provides:

- `allow()`: permit traffic and advance generation only on blocked-to-allowed.
- `block(status, detail)`: deny operations, store status/detail, advance the
  generation, and notify waiters.
- `enforce()`: return `CoreError::NetworkBlocked` when denied.
- `cancelled_since(generation)`: complete when generation changes to blocked.

Every bind, connect, resolve, accept-loop iteration, UDP send, tracker request,
webseed request, and DHT send calls `enforce()`. Each top-level data-plane task
selects normal work against `cancelled_since(start_generation)` so connected
TCP/TLS streams drop on block. The control listener never uses this gate.

Store one injected `Arc<dyn InterfaceProbe + Send + Sync>` on `DaemonRuntime`.
Production injects `OsInterfaceProbe`; tests inject a mutable fake. Extract one
`pub(crate) async fn network_health_tick()` from the loop. The loop only invokes
that operation; tests call it directly without sleeping.

On healthy-to-unhealthy transition, while holding the data-plane transition
lock, perform this exact order:

1. Block the gate immediately.
2. Stop the inbound listener and shared DHT runner.
3. Abort/drop all downloader, metadata, tracker, webseed, and seeder task
   handles; do not await graceful protocol shutdown before dropping sockets.
4. Reconcile byte counters and verified progress already reported by tasks.
5. Set previously active torrents to `network_blocked`, retain prior activity
   for recovery, persist, and publish torrent plus network-status events.

On recovery, update health, allow the gate, reconstruct listener/DHT, and
requeue only work active before the block. Paused and automatically seed-stopped
torrents remain stopped.

Route bind/listen/source-bind failures through a runtime health-report channel.
Such a report blocks the gate and exposes `socket_bind_failed`. Use
`blocked_fail_closed` only when strict policy denies traffic and no more specific
interface/address/namespace/route/DNS/bind status applies. Both statuses require
production-path API tests.

### Required tests

- Config matrix: omitted table/file, strict with path, explicit disabled,
  partial network table, env override, and `--check-config`.
- A mutable fake-probe integration test starts a local transfer, flips the
  required interface healthy-to-missing, calls `network_health_tick()`, and
  proves the gate blocks before teardown, all data-plane registries empty, UDP
  spy sends stop, torrent/API status is blocked, and control API still responds.
- Recovery resumes only formerly active work, not paused/ratio/idle-stopped work.
- Injected bind failure and generic policy denial expose the two formerly
  unreachable statuses.
- Add `scripts/test-network-containment-transition.sh`: a Linux-only privileged
  harness that creates two temporary namespaces joined by a veth pair, runs a
  generated local swarm, binds strict containment to the daemon-side veth,
  proves traffic, deletes that veth during transfer, and proves block/teardown.
  Use cleanup traps, PID-qualified names, no host default-route changes, and no
  external network. Add an Ubuntu CI job that builds normally and grants only
  namespace/link commands the needed `sudo`.

### Documentation updates

- Replace disabled getting-started examples with a strict interface example;
  put explicit disabled mode in a warning for its narrow use.
- Align `README.md`, configuration docs, containment design, deployment docs,
  and samples with omitted-table failure.
- Keep Gluetun config explicitly disabled and explain the shared namespace.
- Add injected and namespace transition tests to `design/testing.md`.
- Record the breaking default in `CHANGELOG.md`; Phase 9 performs the version
  bump.

### Phase 2 definition of done

No implicit configuration permits torrent traffic; every transport observes one
live gate; local traffic stops on injected and real Linux path loss; the control
plane remains available; statuses are reachable; docs agree; and every global
quality gate passes.

## Phase 3: apply browser-origin protection to every control API

**Status: TODO**

**Decision record:** update ADR-0044 and ADR-0049. No new ADR is needed because
this phase restores their stated behavior.

### Required implementation

Extract current `Origin`/`Host`/Fetch Metadata validation into one
`browser_origin_guard` Axum middleware. Build the router as:

- Public health routes that neither mutate nor reveal torrent data.
- All `/api/v1`, `/transmission/rpc`, and `/api/v2` routes under the shared
  origin guard, followed by each surface's existing auth/session middleware.

Apply the guard before handler extraction and before compatibility-enabled
checks. Remove the duplicate guard call from native auth.

The exact policy is:

- Reject `Sec-Fetch-Site: cross-site` and `Sec-Fetch-Site: same-site` with HTTP
  403; only `same-origin`, `none`, or an absent value can continue.
- When `Origin` exists, parse it and require its authority to match `Host` using
  the existing normalized host/port comparison. Intentionally ignore scheme so
  ADR-0044's TLS-terminating reverse-proxy behavior remains valid. Reject
  malformed, opaque, `null`, multi-value, or mismatched origins with 403.
- When both browser headers are absent, continue as a non-browser client to
  normal authentication.
- Do not weaken the rule with CORS. Authentication mode changes credential
  checks only, never browser-origin checks.

Keep native JSON error envelopes and each compatibility surface's documented
error shape/status. Never redirect a rejected API request to the Web UI.

### Required tests

Create one table-driven real-router matrix for authentication enabled/disabled:

- Native add, pause, remove, settings mutation, WebSocket, and SSE.
- Transmission session negotiation and a mutating RPC method.
- qBittorrent login, add, pause/resume, and delete form endpoints.
- Same origin; rejected same-site and cross-site Fetch Metadata; foreign or
  malformed origin; `Origin: null`; and absent browser headers.

Cross-site cases return 403 before mutation and leave fake-daemon counters zero.
Same-origin and headerless authenticated clients retain existing behavior.

### Documentation updates

- Align `design/api.md`, `docs/api.md`, and compatibility docs.
- Add the route matrix to `design/testing.md`.
- Update ADR-0044/ADR-0049 links and implementation notes.
- Record the security correction in `CHANGELOG.md`.

### Phase 3 definition of done

Every browser-reachable control route uses one guard in both auth modes; route
matrices prove no mutation on rejection; docs/ADRs agree; and every global
quality gate passes.

## Phase 4: complete seeding policy, lifecycle, accounting, and live upload shaping

**Status: TODO**

**Decision record:** create
`design/adr/0052-persisted-per-torrent-seeding-policy-and-runtime-lifecycle.md`
and link ADR-0045. New fields are backward-compatible additions to existing
versioned state; do not bump the state version solely for defaulted fields.

### Required domain and persistence model

Add `#[serde(default)] pub seeding: TorrentSeeding` to `Torrent` and expose it
on `TorrentSummary`. Add a serialized `SeedingStatus` enum with these exact
snake-case values:

- `not_eligible`: content is not fully verified.
- `queued`: content is complete and eligible but no seed slot is active.
- `active`: the seeder is accepting/serving peer sessions.
- `stopped_ratio`: the effective ratio target stopped seeding.
- `stopped_idle`: the effective idle target stopped seeding.
- `stopped_manual`: the operator paused a complete torrent.

Add `#[serde(default)] pub seeding_status: SeedingStatus` to `Torrent`; default
is `not_eligible`. Persist both fields and expose them in native list/detail
responses. State without these fields restores with inherited global limits and
a status recomputed from verified completion and lifecycle before scheduling.

Keep `TorrentState` as the coarse lifecycle state and use these transitions:

- Fully verified and waiting for a seed slot: `completed` + `queued`.
- Successfully started seeder: `seeding` + `active`.
- Ratio/idle stop: `completed` + its corresponding stopped status.
- Manual pause of complete content: `paused` + `stopped_manual`.
- Incomplete download/recheck: `not_eligible`.
- Containment failure: `network_blocked`, preserving prior seeding status for
  Phase 2 recovery.

Derive `active_seeds` from the live seeder-task registry under the same
synchronization as lifecycle transitions, not an independent enum snapshot.
Test the invariant that each live seeder maps to `seeding` + `active` and every
such state maps to one live seeder.

Calculate `TorrentFile.bytes_completed` from verified pieces. Give a file bytes
only from verified piece ranges intersecting its byte range, including boundary
pieces. Recompute after restore, piece completion, recheck, metadata resolution,
reset, and file-list replacement. Never multiply torrent fraction by file size.

### Required API and Web UI behavior

Add this native endpoint:

```text
PUT /api/v1/torrents/:info_hash/seeding
Content-Type: application/json

{
  "ratio_limit": 1.5,
  "idle_limit": 1800,
  "seed_forever": false
}
```

All keys are required because `PUT` replaces the policy. `ratio_limit` is a
finite non-negative number or `null`; `idle_limit` is a non-negative integer
number of seconds or `null`; `null` means inherit global. `seed_forever: true`
overrides both effective targets without erasing stored values. Reject negative,
non-finite, out-of-range, missing, or unknown fields as `invalid_argument`.

Persist before success. Re-evaluate an active seeder immediately: a newly met
target stops it; relaxing an automatic stop queues complete content. Never
auto-resume a manually paused torrent.

Show ratio, uploaded bytes, stored overrides, effective targets,
`seed_forever`, and `seeding_status` in torrent details. The form distinguishes
inherit (`null`) from explicit zero, calls the native endpoint, and displays
server validation without optimistic state drift.

Map Transmission and qBittorrent fields from the same stored/effective policy,
but only fields already declared supported in compatibility docs. Do not invent
unsupported adapter options.

### Required live limiter architecture

Rename `engine_limiters` to `torrent_limiters`. Create one `Arc<RateLimiter>`
per torrent and retain it across downloader completion, seed-slot waits,
pause/resume, and downloader-to-seeder transition. Pass the same instance to
engine and seeder. Remove it only on torrent removal/reset or discarded restore.
`set_torrent_limits` updates this shared limiter so an active seeder observes
new capacity without restart.

Do not create a replacement limiter in seeder startup. Keep global upload
limiting layered with the per-torrent limiter so a write obtains both.

### Required tests

- Global ratio/idle inheritance, each override, explicit zero, seed-forever
  precedence, and validation failures.
- Backward restore with absent fields and round-trip restore for each status.
- Completion to queued seeding, slot acquisition to active, ratio/idle stop,
  manual pause/resume, target relaxation, containment recovery, and restart.
- `active_seeds`/seeder registry consistency on start, stop, error,
  cancellation, and removal.
- Single- and multi-file boundary-piece fixtures proving exact per-file bytes
  before and after recheck/restore.
- An active local upload whose per-torrent limit changes without seeder restart;
  measure deterministic test-clock windows and assert old/new bounded rates
  with tolerance documented in the test.
- Native endpoint/list/detail, UI DOM/state rendering, and documented
  compatibility mappings.

### Documentation updates

- Update requirements, native/compatibility API, configuration, architecture,
  and Web UI/operator docs with exact inheritance and transitions.
- Add local upload, file-boundary, persistence, and lifecycle matrices to
  `design/testing.md`.
- Keep associated `design/v1-completion-tracker.md` rows incomplete until
  production-path tests pass.
- Record seeding, file-accounting, and live-limit capabilities in
  `CHANGELOG.md`.

### Phase 4 definition of done

Policy survives restart and is editable through API/UI; seeding state/stats
match task reality; file progress is exact; active seeders respond to limit
changes; compatibility mappings are honest; and every global quality gate
passes.

## Phase 5: enforce one process-wide peer connection budget

**Status: TODO**

**Decision record:** create
`design/adr/0053-process-wide-peer-session-permit-pool.md` and link ADR-0046
and ADR-0047.

### Required implementation

Replace global-share arithmetic with one runtime-owned `PeerPermitPool` backed
by `tokio::sync::Semaphore`:

- `bandwidth.max_peers == 0` means unlimited and returns an uncounted permit.
- A nonzero value creates exactly that many owned permits.
- Acquire before initiating outbound peer TCP/uTP and hold until the peer
  session drops.
- An accepted inbound peer uses `try_acquire_owned`; without a permit, close it
  before handshake and increment a denied counter.
- Metadata-fetch and all serial, parallel, and endgame peer sessions consume
  permits. Trackers, webseeds, DHT nodes, and DNS do not because this setting is
  specifically the peer connection limit.
- `max_peers_per_torrent` remains separate. A session satisfies both caps.

Delete `effective_peer_worker_limit` global division. Engine worker count may
still use the per-torrent cap; workers wait for a global permit before opening a
socket. Never hold a permit while waiting for discovery or retry.

Treat live `max_peers` changes as ADR-0047 data-plane reconstruction: block new
starts, stop peer-bearing tasks, construct a new pool, restart eligible work,
then commit settings. Do not resize a semaphore in place. Failed reconstruction
restores the old config and old-size pool.

Expose `peer_limit`, `peer_permits_in_use`, `peer_permits_available`, and
`peer_sessions_denied` diagnostics. In unlimited mode, limit is `0`, available
is `null`, and in-use still reports observed sessions.

### Required tests

- Five active torrents with `max_peers = 2` never exceed two total sessions.
- Mixed inbound/outbound sessions share the cap.
- Metadata, normal, parallel/endgame, uTP, and seeding paths hold lifetime
  permits.
- Cancellation, connect/handshake error, normal EOF, and task panic release an
  owned RAII permit; do not balance counters manually.
- Unlimited mode, composition with per-torrent cap, and diagnostics.
- Successful/failed ADR-0047 reconfiguration commit/roll back and never exceed
  either applicable nonzero cap.
- A generated local swarm samples diagnostic concurrency during the run, not
  only at completion.

### Documentation updates

- Correct inclusion/exclusion semantics in configuration, architecture,
  diagnostics, and API docs.
- Add permit-lifetime and mixed-direction cases to `design/testing.md`.
- Link the new ADR from ADR-0046 and ADR-0047.
- Record process-wide enforcement in `CHANGELOG.md`.

### Phase 5 definition of done

Every peer path owns a permit; mixed live sessions cannot exceed the cap;
reconfiguration is transactional; diagnostics and docs agree; and every global
quality gate passes.

## Phase 6: make watch-folder ingestion stable, idempotent, and transactional

**Status: TODO**

**Decision record:** create
`design/adr/0054-watch-folder-stability-idempotence-and-import-atomicity.md`.

### Required scanner behavior

Move the complete directory walk into `tokio::task::spawn_blocking`. Use
`symlink_metadata`, skip all symbolic links, and never descend through symlinked
directories. Return sorted paths for deterministic history.

Store an in-memory observation map keyed by a lexically normalized absolute
path relative to the watch root; do not use `canonicalize`, which follows
symlinks. An observation holds length, modified timestamp, consecutive stable
scans, and last processed fingerprint. The fingerprint is `(length,
modified_timestamp)` and is only an observation key, not a security hash.

Use this state machine:

1. First sighting records metadata and does not import.
2. Changed length/timestamp resets stable scans to one and does not import.
3. Two consecutive scans with identical metadata make the file eligible.
4. A successfully processed fingerprint is skipped until metadata changes.
5. Remove observations for paths absent from the latest successful root scan.

After restart, first observe again. If an unchanged file is already registered,
the second scan treats it as successful duplicate, executes success action,
marks processed, and does not repeat. Add no durable watch ledger.

Apply Phase 1's size bound before allocation. Re-check metadata after bounded
read; if length/timestamp changed, discard bytes, update observation, and wait
for stability again.

### Required import transaction and outcomes

Refactor API and watch add paths to one daemon mutation primitive, under the
existing mutation serialization lock, in this order:

1. Parse and run storage/network preflight without mutation.
2. Snapshot prior registry/queue entries for the hash.
3. Insert torrent and queue entry in memory.
4. Persist durable state.
5. On persistence error, restore both snapshots before releasing the lock and
   return the original error.
6. Only after persistence succeeds, publish events and schedule reconciliation.

A duplicate is `success: true`, `duplicate: true`, uses the existing hash, and
executes success action. It never changes the existing torrent, queue position,
labels, path, or settings.

Only `Bencode`, `MalformedTorrent`, `InvalidInfoHash`, and `Parse` are permanent
input failures and execute failure move. Storage, I/O, persistence, containment,
and internal errors are transient: leave source, do not mark processed, and
retry after later stability.

Post-action rules are:

- Never overwrite an archive/failure destination.
- Create a configured destination directory if absent.
- If destination exists or move/delete fails, keep the primary import outcome,
  add `post_action_error`, mark processed, and do not re-import. The operator
  resolves the source manually.
- `leave` marks the fingerprint processed after success/duplicate.

Extend `ImportResult` with `post_action_error: Option<String>` and stable
`outcome`: `imported`, `duplicate`, `permanent_failure`, or
`transient_failure`. Retain only the newest 10,000 results in insertion order;
evict oldest on insertion. Do not persist this operational history.

### Required tests

- Multi-scan partial copy and a metadata change during read.
- `leave` imports once; changed replacement processes once again.
- Duplicate after restart runs action once without changing existing torrent.
- Injected persistence failure rolls registry/queue back and publishes/schedules
  nothing.
- Permanent errors move; transient errors stay/retry.
- Destination collision never overwrites and reports `post_action_error`.
- Sorted recursive/non-recursive scans, ignored symlinks, and observation
  removal for disappeared paths.
- History entry 10,001 evicts entry 1.
- API add uses the same rollback primitive and retains response contract.

### Documentation updates

- Specify stability, restart, duplicate, error classification, non-overwrite,
  and history behavior in design/operator configuration and Web UI docs.
- Add failure injection and partial-copy cases to `design/testing.md`.
- Document the shared durable add transaction in `design/architecture.md`.
- Keep watch rows in `design/v1-completion-tracker.md` incomplete until tests
  pass.
- Record stable/idempotent import in `CHANGELOG.md`.

### Phase 6 definition of done

No unstable/oversized file is parsed; unchanged files have one terminal result
per run; duplicates are harmless; persistence leaves no ghost state; post
actions never overwrite; history is bounded; and every global gate passes.

## Phase 7: make contained HTTP framing, redirects, ranges, and tracker scrape complete

**Status: TODO**

**Decision record:** create
`design/adr/0055-contained-http1-client-framing-and-redirect-policy.md` and link
ADR-0014 and ADR-0018.

### Required dependency and transport decision

Keep connection creation in the containment binder. Use Hyper only as an
HTTP/1.1 codec over an already-connected contained TCP or rustls-wrapped stream;
do not use a Hyper connector, resolver, pool, or general client that can open
sockets.

Enable Hyper's `client` feature and add direct workspace dependencies on
`hyper-util` with its Tokio adapter feature and `http-body-util`. Pin major
versions consistently with the existing Hyper lock. Record all direct uses and
MIT/Apache-compatible licensing in `THIRD_PARTY_LICENSES.md`. Do not add
`reqwest`.

Replace `parse_http_response` and read-to-EOF tracker/webseed parsing with a
shared `ContainedHttpClient`. Each request must:

- Resolve/connect through `NetworkBinder`, perform TLS over that stream when
  needed, and wrap only that stream in Hyper's Tokio I/O adapter.
- Send HTTP/1.1 origin-form and a `Host` authority retaining non-default port
  and brackets around IPv6 literals.
- Let Hyper decode transfer coding and framing, reject body/connection protocol
  errors, and stream decoded chunks into a bounded accumulator.
- Apply the existing connect timeout and one 30-second overall
  request/response-body timeout, returning the existing typed timeout error.
- Send no implicit cookies/authorization and retain no connection pool.

### Required redirect and response policy

Follow at most five redirects and detect a loop before the sixth request.
Accept only `http`/`https`, resolve relative `Location` against current URL,
allow HTTP-to-HTTPS, and reject HTTPS-to-HTTP. Cross-host redirects are allowed
but repeat contained DNS/connect/TLS for the new host. Do not forward caller
authorization or cookies. Preserve `Range` on webseed GET redirects.

Tracker announce accepts only final 2xx and at most 2 MiB decoded body. A 3xx
without valid permitted `Location`, 4xx, or 5xx is a tracker failure with status
context.

Webseed accepts only final 206. Parse `Content-Range`; require start/end exactly
equal the requested inclusive range and body length equal that range. Bound
decoded bytes to expected length and fail on the first excess byte. Do not
accept 200 for a range request.

### Required tracker scrape behavior

Implement supported HTTP/HTTPS scrape through the same contained client. Derive
a scrape URL only when the final path component begins with `announce`; replace
that prefix with `scrape` while preserving suffix (`announce.php` becomes
`scrape.php`). Other shapes are `unsupported`, not failed.

Send one binary percent-encoded `info_hash` parameter per requested torrent.
Parse BEP 48 `files` through Phase 1's bounded decoder. Update a tracker's
complete, incomplete, and downloaded counts only for an exact matching 20-byte
key. Missing/malformed matching data is failure and does not overwrite the last
successful values. Expose last scrape status/time/counts in tracker API/detail
and the Web UI table. Do not claim UDP scrape support without separate
requirements and acceptance tests.

### Required tests

Use local contained HTTP and rustls HTTPS fixtures for:

- Content-Length, chunked, legal close-delimited, truncated, malformed chunk,
  and decoded over-cap bodies.
- Tracker 2xx, redirect, loop, sixth redirect, bad/missing `Location`,
  cross-host, HTTPS upgrade, downgrade rejection, 4xx, and 5xx.
- Non-default-port and IPv6-literal authorities.
- Webseed exact 206 with Content-Length/chunked, wrong/missing Content-Range,
  200, short body, and one-byte excess.
- A binder spy proving every redirect resolves/connects through containment and
  no general client/raw socket is used.
- Scrape derivation for `announce`, `announce.php`, relative paths, and
  unsupported names; matching/missing/malformed dictionaries; preservation of
  last-success data; API and UI rendering.

All fixtures use generated loopback/namespace data, never external DNS or HTTP.

### Documentation updates

- Document framing, redirects, downgrade, caps, and range policy in
  architecture, containment, API, and operator docs.
- Document HTTP/HTTPS scrape and explicit UDP scrape status in requirements,
  tracker/API docs, and Web UI guide.
- Add the local protocol matrix to `design/testing.md`.
- Update licenses, ADR-0014/ADR-0018 links, and `CHANGELOG.md`.
- Correct scrape evidence in `design/v1-completion-tracker.md` only after
  production-path tests pass.

### Phase 7 definition of done

No tracker/webseed parser depends on EOF for framed messages; chunked, redirect,
authority, cap, and range tests pass over binder-provided plain/TLS streams;
scrape is real/bounded; dependencies are recorded; and every global gate passes.

## Phase 8: decompose oversized modules and remove duplicate daemon compilation

**Status: TODO**

**Decision record:** no ADR is required for this behavior-preserving refactor.
If implementation changes a public path or architectural decision, stop and add
an ADR instead of hiding that change in the refactor.

### Required Rust module layout

Replace the two daemon monoliths with these ownership boundaries, preserving
public paths through each `mod.rs`:

```text
crates/swarmotterd/src/daemon/
  mod.rs             runtime type and public facade
  construction.rs    constructors and shared-resource wiring
  lifecycle.rs       add/remove/pause/resume/recheck and task completion
  scheduler.rs       queue reconciliation and retry policy
  seeding.rs         seed slots, policy, and seeder lifecycle
  settings.rs        live configuration transaction
  watch.rs           watch observations and import orchestration
  containment.rs     health tick, gate transitions, and recovery
  diagnostics.rs     stats, resource snapshots, and events
  persistence.rs     snapshots, restore, and mutation rollback
  tests.rs           tests grouped in submodules by owner

crates/swarmotterd/src/engine/
  mod.rs             engine type, configuration, and facade
  discovery.rs       tracker, DHT, PEX, and candidate merging
  peer_session.rs    handshake, protocol loop, and permit lifetime
  download.rs        serial scheduling and verification
  parallel.rs        parallel scheduling
  endgame.rs         duplicate-request endgame logic
  webseed.rs         range scheduling through ContainedHttpClient
  progress.rs        accounting/checkpoint messages
  tests.rs           tests grouped in submodules by owner
```

Split `crates/swarmotter-api/src/handlers/torrents.rs` into
`handlers/torrents/{mod.rs,add.rs,query.rs,lifecycle.rs,settings.rs,bulk.rs,tests.rs}`.
Keep route paths, handler visibility, envelopes, and test imports stable through
`mod.rs` re-exports.

Delete daemon module declarations from `swarmotterd/src/main.rs`; import the
library facade with `use swarmotterd::{daemon, logging};` and fully qualified
library paths elsewhere. Daemon unit tests must compile once under the library
target.

No production Rust file in these decomposed areas may exceed 2,000 formatted
lines. This is a partitioning limit, not permission to compress code or create
a generic dumping-ground utility module.

### Required Web UI module layout

Keep the no-framework design. Make `app.js` an ES-module entry and split into:

```text
assets/js/api.js       fetch, envelope, and safe request helpers
assets/js/state.js     client state and event subscriptions
assets/js/torrents.js  filtering, sorting, selection, and bulk actions
assets/js/details.js   details, files, peers, trackers, and seeding form
assets/js/settings.js  settings forms and validation
assets/js/events.js    event/log rendering
assets/js/ui.js        dialogs, notices, formatting, and DOM helpers
```

Change the index script to `type="module"`, embed/route every module from
`swarmotter-web/src/lib.rs` with JavaScript content type, and retain current CSP
`script-src 'self'` without inline exceptions. Only the entry point composes
feature modules, preventing cyclic imports. No JavaScript module may exceed
1,000 lines.

### Required tests and safeguards

- Move tests to owning modules; do not delete/weaken them or replace an
  integration test with a helper test.
- Add compile/import coverage for prior public library paths.
- Assert every UI module route returns 200, JavaScript content type, CSP, and
  expected cache policy.
- Run `node --check` on every JavaScript file and retain DOM/UI tests.
- Compare test lists before/after and document any one-for-one rename in phase
  evidence.

### Documentation updates

- Update module ownership diagrams/paths in `design/architecture.md` and
  contributor docs.
- Update Web UI asset/security docs for ES modules.
- Add no changelog item for file movement alone. Any user-visible correction
  belongs in its responsible functional phase.

### Phase 8 definition of done

Prescribed boundaries/size limits hold; the binary uses the library facade; no
test is duplicated/lost; public imports, routes, assets, CSP, and behavior remain
stable; and every global gate passes.

## Phase 9: reconcile requirement evidence and close release readiness

**Status: TODO**

**Decision record:** follow `design/VERSIONING_GUIDE.md`. Add no architectural
ADR unless the audit discovers a new decision rather than missing execution.

### Required traceability audit

Audit every checkbox in `design/requirements.md` and every completion row in
`design/v1-completion-tracker.md`. Each acceptance criterion must cite:

1. The production entry point or UI/API operation exposing behavior.
2. The named automated acceptance test invoking that path.
3. The operator/developer document describing the same behavior.

If any is absent, keep or return the item to documented incomplete state. A
type, enum, pure helper, mock-only call, or prose claim is not production
evidence.

Explicitly close these reachability items:

- `TorrentState::TrackerError`: when discovery has a configured tracker, all
  attempted trackers fail, and no usable DHT/PEX/direct/webseed candidate
  exists, set `tracker_error` with the last tracker error. Manual reannounce or
  resume clears/retries. A successful non-tracker source prevents this state.
- Every `NetworkContainmentStatus`: exercise production mapping or remove a
  status without distinct meaning and update schemas/docs.
- Per-file completed bytes: cite Phase 4 verified-range production tests.
- Tracker scrape: cite Phase 7 contained HTTP/HTTPS scrape and UI/API tests.
- Watch stability: cite Phase 6 daemon scanner, not only its pure helper.
- Active seeds/live limits: cite Phase 4 task-registry and upload tests.

Search source/docs for `complete`, `implemented`, `supported`, and checked
tracker rows. Correct each claim lacking the three-part evidence.

### Required compatibility and version action

Phase 2 turns an insecure implicit default into startup rejection, a breaking
configuration change. After every phase passes, use `scripts/bump_version.sh`
for the next major with minor/patch zero. At baseline `1.3.1`, run:

```bash
scripts/bump_version.sh v2.0.0
```

If version changed before Phase 9, mechanically use
`v<current-major+1>.0.0`; do not choose minor/patch. Verify every file listed by
`design/VERSIONING_GUIDE.md`, update `Cargo.lock`, and add migration notes:
existing users configure a strict path or explicitly acknowledge disabled mode.
Do not tag, commit, push, or publish without a separate maintainer request.

### Required final acceptance runs

Run every global gate below, both ignored scale harnesses, and the privileged
Linux containment transition. Record command, commit SHA, toolchain, and result
as phase evidence. Test data remains generated/local.

Inspect final diff for:

- No torrent socket/DNS/HTTP creation outside containment.
- No unbounded read or attacker-sized allocation on metadata/HTTP/watch paths.
- No new production `unwrap`/`expect` where typed error is possible.
- No piracy-oriented text, links, trackers, magnets, or fixtures.
- No duration estimate or MVP language.
- Every dependency recorded and new Rust file SPDX-tagged.

### Documentation updates

- Align requirements, completion tracker, all affected design/API/config/test
  docs, README/operator docs, accepted ADRs, and `CHANGELOG.md`.
- Add breaking containment migration to release and getting-started docs.
- Fill completion evidence in every phase-map row and mark Phase 9 last.

### Phase 9 definition of done

Every criterion has production/test/document evidence; overstated claims are
fixed; prior phases are `COMPLETE`; prescribed version/migration is applied;
final acceptance passes; no publish action occurred; and every global gate
passes.

## Global definition of done and quality gates

These gates apply to every implementation phase. A phase cannot be `COMPLETE`
while an applicable gate is skipped/failing. Run from repository root.

### Code quality and tests

```bash
cargo fmt --all -- --check
cargo check --locked --workspace --all-targets --all-features
cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
cargo test --locked --workspace --all-targets --all-features
cargo +1.88.0 check --locked --workspace --all-targets --all-features
```

Phase 9 also runs explicit scale/soak cases:

```bash
cargo test --locked -p swarmotterd ignored_thousand_mixed_state_torrents_keep_scheduler_bounds -- --ignored
cargo test --locked -p swarmotter-api --test scale_harness -- --ignored
```

Phase 2 and Phase 9 also run the Linux harness:

```bash
cargo build --locked -p swarmotterd
sudo scripts/test-network-containment-transition.sh "$PWD/target/debug/swarmotterd"
```

### Web, documentation, packaging, and diff checks

```bash
find crates/swarmotter-web/assets -type f -name '*.js' -exec node --check {} \;
mdbook build
GLUETUN_ENV_FILE=gluetun.env.example docker compose --env-file deploy/.env.example -f deploy/compose.yml config
git diff --check
```

For container changes, also build the affected image locally without secrets in
arguments/layers. For dependency changes, inspect
`cargo tree -p <affected-package>` and verify `THIRD_PARTY_LICENSES.md`.

### Review evidence required in the phase map

For each completed phase, record:

- ADR path or existing ADR updated.
- Production-path test names and source paths.
- Documentation paths updated.
- Quality-gate result and commit SHA.
- Intentionally unchanged compatibility behavior and its proving test.

The phase map is the operational handoff. Without that evidence, the phase
remains `TODO` even when implementation exists.

// SPDX-License-Identifier: Apache-2.0

//! Web support for SwarmOtter.
//!
//! This crate is a placeholder during repository setup. It will eventually
//! serve the practical Web UI described in `design/architecture.md`. Per
//! project policy, the Web UI consumes the same API exposed to external
//! automation and prioritizes function over form.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        let _ = ();
    }
}
